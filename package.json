{
  "name": "tic-tac-toe-browser",
  "version": "1.0.0",
  "description": "Browser-based Tic-Tac-Toe game with single-player and multiplayer modes in Spanish and French",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "dev:server": "nodemon server/index.js",
    "dev:client": "cd client && npm start",
    "install:all": "npm install && cd client && npm install"
  },
  "keywords": ["tic-tac-toe", "socket.io", "multiplayer", "game"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
